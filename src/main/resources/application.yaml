spring:
  application:
    name: waycourier
  datasource:
    url: jdbc:postgresql://localhost:5432/location-service
    username: admin
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  security:
    user:
      name: admin
      password: admin